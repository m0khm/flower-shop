version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: always

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    env_file: 
      - .env
    ports:
      - "4000:4000"
    restart: always
